require 'spec_helper'

describe AnsibleQa do
  it 'has a version number' do
    expect(AnsibleQa::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
